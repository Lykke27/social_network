import {FriendsPageType} from "./state";

const friendsReducer = (state: FriendsPageType, action: any) => {
    switch (action.type) {
        default: return state;
    }
}

export default friendsReducer;